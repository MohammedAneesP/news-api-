import 'package:flutter/material.dart';

class Entertainmet extends StatelessWidget {
  const Entertainmet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Entertianment")),);
  }
}