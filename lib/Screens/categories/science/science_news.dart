import 'package:flutter/material.dart';

class Sceince extends StatelessWidget {
  const Sceince({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Science"),
      ),
    );
  }
}